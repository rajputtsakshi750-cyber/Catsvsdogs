This project is based on image classification using Convolutional Neural Network (CNN).
The model is trained to classify images into two categories:
Cat 🐱 and Dog 🐶.
We use deep learning techniques to extract features from images and predict whether the given image is a cat or a dog.
